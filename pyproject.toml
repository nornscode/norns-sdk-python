[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "norns-sdk"
version = "0.8.0"
description = "Python SDK for Norns — durable agent runtime on BEAM"
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
dependencies = [
    "websockets>=12.0",
    # 1.96.1 shipped cp310-only wheels, breaking installs on 3.11+
    "litellm>=1.60.0,!=1.96.1",
    "httpx>=0.27.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "respx>=0.21",
]

[tool.hatch.build.targets.wheel]
packages = ["norns"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
